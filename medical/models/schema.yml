# In models/schema.yml

version: 2

models:
  - name: cleaned_data
    columns:
      - name: message_id
        tests:
          - not_null
      - name: telegram_id
        tests:
          - unique
      - name: date
        tests:
          - not_null
